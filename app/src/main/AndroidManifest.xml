<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ToList">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="util.YololistApi"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yololist"
        tools:replace="android:icon">
        <activity android:name="com.example.ToList.UpdateYololistActivity"></activity>
        <activity android:name="com.example.ToList.YoloListActivity" />
        <activity android:name="com.example.ToList.ArchiveActivity" />
        <activity android:name="com.example.ToList.PostYololistActivity" />
        <activity android:name="com.example.ToList.userprofile" />
        <activity android:name="com.example.ToList.RegisterActivity" />
        <activity
            android:name="com.example.ToList.ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name="com.example.ToList.SecondActivity" />
        <activity android:name="com.example.ToList.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>